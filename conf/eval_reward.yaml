defaults:
  - eval_reward_schema
  - _self_

hydra:
  sweeper:
    params: {}

n_envs: 500

encoder:
  model: cnnclip

decoder:
  hidden_dim: 128
  num_layers: 2
  num_reward_classes: 5
  cnn_reward_enum_onehot: true
