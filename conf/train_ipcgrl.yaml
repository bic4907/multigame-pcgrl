defaults:
  - cpcgrl
  - _self_

use_nlp: true
render_freq: -1
saves_dir: saves
init_ckpt_path: null

# dataset_reward_enum: 0  # 단일 enum (0=region). null이면 전체 5종 로드.
dataset_reward_enum: null
longtail_cut: true

encoder:
  model: mlp
  trainable: true
  ckpt_path: null  # 사전학습 인코더 경로. null이면 scratch. 지정 시: encoder.ckpt_path=<path>

hydra:
  sweeper:
    params: {}

