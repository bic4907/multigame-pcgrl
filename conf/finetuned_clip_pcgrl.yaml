defaults:
  - finetuned_clip_pcgrl_schema
  - _self_

hydra:
  sweeper:
    params: {}

n_envs: 600

encoder:
  model: clip
  # text CLIP checkpoint directorytext (pretrained_encoders/<ckpt_name>/ckpts)
  # text: "finetuned-clip-game-all_exp-def_0" text  textpath ckpt_path
  ckpt_dir: pretrained_encoders
  ckpt_name: null
  ckpt_path: null
  trainable: false

