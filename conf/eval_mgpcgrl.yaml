defaults:
  - eval_mgpcgrl_schema
  - _self_

hydra:
  sweeper:
    params: {}

n_envs: 600

encoder:
  model: cnnclip

decoder:
  hidden_dim: 128
  num_layers: 2
  num_reward_classes: 5
  cnn_reward_enum_onehot: true
